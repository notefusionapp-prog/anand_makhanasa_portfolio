# ==============================================================================
# Single Page Application (SPA) Client-Side Routing Fallback
# All non-static asset routes are served index.html with HTTP 200
# Form submissions via POST / are captured by Netlify Forms engine
# ==============================================================================
/*  /index.html  200

